# Local / generated
guide-extracted.txt
*.bak
*.log
*.tmp
*.swp
.env
.env.*
__pycache__/
*.pyc
.DS_Store

mirror/
var/
skel/

.vscode/
.idea/

# Real DP host collection archives (do not commit)
dp-upgrade-readiness-*.tar.gz
dp-upgrade-preflight-*.tar.gz
ubuntu-upgrade-discovery-*.tar.gz
ubuntu-upgrade-discovery-*.tar.gz.sha256


# Local test smoke outputs
tests/smoke-results/

# Upgrade-discovery workspace exports: manifests only (never payloads/caches)
artifacts/upgrade-discovery/**/*.deb
artifacts/upgrade-discovery/**/*.udeb
artifacts/upgrade-discovery/**/*.tar
artifacts/upgrade-discovery/**/*.tar.gz
artifacts/upgrade-discovery/**/*.xz
artifacts/upgrade-discovery/**/*.gz
artifacts/upgrade-discovery/**/*.bz2
artifacts/upgrade-discovery/**/runtime/
artifacts/upgrade-discovery/**/deb-cache/
artifacts/upgrade-discovery/**/payload/
artifacts/upgrade-discovery/**/responses/
artifacts/upgrade-discovery/.staging-*/
artifacts/upgrade-discovery/.replace-*/
artifacts/upgrade-discovery/.old-*/
artifacts/upgrade-discovery/.index.tsv.tmp.*

# Client build outputs (regenerate with build-client-xenial-to-bionic)
artifacts/client/
# Unsigned test-only client builds (never deploy / never nginx)
artifacts/client-unsigned-test/
# Manifest signing private key (public .gpg may be committed; install-time
# /etc/ubuntu-mirror/client-signing/private.gpg is never in this repo)
config/client-signing/*.private.gpg
config/client-signing/*private*
config/client-signing/private.gpg
**/client-signing/private.gpg

artifacts/upgrade-discovery/
# Multi-profile discovery: commit lightweight manifests only (never deb-cache/payloads)
artifacts/upgrade-discovery-profiles/**/runtime/
artifacts/upgrade-discovery-profiles/**/deb-cache/
artifacts/upgrade-discovery-profiles/**/payload/
artifacts/upgrade-discovery-profiles/**/*.deb
aws-upgrade-discovery-*.tar.gz
artifacts/recovery/
artifacts/logs/
artifacts/client/nginx-deploy/
artifacts/client/build-summary.json
ubuntu-mirror-automation/
